---
output: github_document
---

# cofad TODO

## Data and numerical verification

- [ ] Systematically compare every packaged example and expected statistic with
  the Rosenthal and Sedlmeier source tables. Record intentional differences such
  as rounding, corrected apparent book errors, and one- versus two-sided tests.
- [ ] Add source/provenance scripts for packaged data that cannot currently be
  regenerated directly from `data-raw/`.

## Design detection

- [ ] Exercise `detect_design()` on additional incomplete, unbalanced, and
  irregular real-world data. Keep ambiguous cases conservative and document
  cases that require manual selection.
- [ ] Add UI explanations of the detector diagnostics when a suggestion has low
  confidence, rather than displaying only the aggregate confidence percentage.
- [ ] In Mimosa, benchmark the existing structural ID/level detector against an
  optional covariance/ICC diagnostic. Treat covariance as supporting evidence,
  not as a replacement for crossing and within-group constancy checks.

## Shiny and webR

- [ ] Manually acceptance-test editing the model and contrast tables after
  repeated file uploads, especially when two files have identical column names.
- [ ] Add browser regression coverage for table edits, validation messages, the
  detailed F table, and the variance-partition figure once the browser test
  runner is sufficiently reliable.
- [ ] Monitor the deployed Shinylive/webR build and measure startup on several
  desktop/mobile devices and real network connections. The August 2026 local
  baseline was 26.9 seconds to controls and 32.9 seconds to an example model
  table (22.4/26.8 seconds on a cached reload).
- [ ] Remove the obsolete sortable/drag-and-drop helper code after confirming it
  is not used by downstream code.

## Testing and releases

- [ ] Raise coverage in the remaining branches of `server.R` and
  `calc_contrast.R`, prioritizing error handling and malformed uploaded data.
- [ ] Consider enforcing a minimum coverage percentage in CI after the new
  baseline has proved stable.
- [ ] Run release checks on current, old-release, and development R, including
  Windows and macOS, before the next CRAN submission.

## Longer-term ideas

- [ ] Add confidence intervals for correlation-based effect sizes.
- [ ] Investigate a jamovi integration.
- [ ] Improve packaged-data names without breaking existing users; prefer aliases
  and deprecation periods over renaming objects directly.

## Completed in the August 2026 audit

- [x] Preload all categorized examples with the planned contrasts used in their
  books, papers, or documented study hypotheses; automatically activate the
  published competing hypotheses where available.
- [x] Add opt-in favored-versus-rival contrast columns to the Shiny and webR
  interfaces, using standardized difference weights and reversible single-
  contrast behavior.
- [x] Restore editable table-based model and contrast inputs.
- [x] Automatically suggest between-, within-, ID-, and outcome-variable roles.
- [x] Add detailed SS/QS, F, effect-size, and variance-partition output.
- [x] Load packaged examples in the Shiny app.
- [x] Add in-process Shiny tests and ordinary package coverage.
- [x] Add an optional Shinylive/webR build.
- [x] Configure automatic GitHub Pages deployment for the webR app.
- [x] Add the 2025 tutorial and JOSS software citation.
- [x] Add APA 7 plain-text, HTML, and BibTeX copy buttons to the app, and return
  both required references from `citation("cofad")`.
- [x] Add categorized between, within, and mixed example data sets to the app.
- [x] Add participant-level r scores as a pattern-fit alternative to L scores,
  with Rosenthal regression tests and usage guidance.
- [x] Add corrected `rosenthal_tbl68_mixed`, in which each participant belongs
  to one between group, with a regression test; retain the historically
  packaged `rosenthal_tbl68` unchanged for backward compatibility.
